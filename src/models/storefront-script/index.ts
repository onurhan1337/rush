export interface StorefrontScript {
  id: string;
  authorizedAppId: string;
  storefrontId: string;
  scriptId: string;
  version: string;
  installedAt: string;
  updatedAt: string;
  deleted: boolean;
}
